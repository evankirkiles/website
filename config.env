APP_NAME=Evan Kirkiles' Website

# Make this strong, generate with Python3 command:
# $ python3 -c "import secrets; print(secrets.token_hex(16))"
SECRET_KEY=9d3bd85f3429e3b5e2fafcd931e28f05

# Which version of the app currently running? (development or production)
FLASK_CONFIG=development
SSL_DISABLE=False  (but really up to you)